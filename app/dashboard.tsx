"use strict";

import { Fragment, } from "react";

const Template = ({ children, }) =>
{
    return (

        <Fragment>
            {children}
        </Fragment>
    );
};

export default Template;
