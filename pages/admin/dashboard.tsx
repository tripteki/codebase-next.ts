"use strict";

import { Fragment, } from "react";
import { Button, } from "@material-tailwind/react";

const Template = () =>
{
    return (

        <Fragment>
            <Button placeholder={undefined}>Test</Button>
        </Fragment>
    );
};

export default Template;
